{% form_theme articleForm 'bootstrap_3_layout.html.twig' %}

{{ form_start(articleForm) }}
	{{ form_row(articleForm.name) }}
	{{ form_row(articleForm.title) }}
	{{ form_row(articleForm.url) }}
	{{ form_row(articleForm.icon) }}
{{ form_end(articleForm) }}